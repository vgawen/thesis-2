\documentclass[UTF8, a4paper, 10.5pt, twocolumn, fontset=none]{ctexart}

\usepackage[top=2.0cm, bottom=2.0cm, left=1.8cm, right=1.8cm, columnsep=0.6cm]{geometry}
\setlength{\columnseprule}{0pt}

\usepackage{fontspec}
\usepackage{xeCJK}
\setCJKmainfont[BoldFont={PingFang SC Semibold}, ItalicFont={Kaiti SC}]{Songti SC}
\setCJKsansfont{PingFang SC}
\setCJKmonofont{STSong}
\xeCJKsetup{CJKecglue={\hskip 0.15em plus 0.05em minus 0.05em}}
\usepackage{booktabs}
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, shapes.geometric, fit, backgrounds}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{titlesec}
\usepackage[numbers,sort&compress]{gbt7714}

\captionsetup{font=small, labelsep=quad}
\titleformat{\section}{\large\bfseries}{\thesection}{0.6em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection}{0.5em}{}

\sloppy

\hypersetup{
  colorlinks=true,
  linkcolor=black,
  citecolor=black,
  urlcolor=blue!60!black,
}

\title{React 应用 Playwright 测试隐性误修复研究}

\author{
  第一作者\thanks{作者简介：第一作者（19XX—），性别，民族，籍贯（省市），职称，学位（学历），研究方向：软件工程、自动化测试。\\
  通讯作者 E-mail：author@example.com。本研究受 XXX 基金（编号 XXXXX）资助。}\\
  \small 作者单位（投稿前补全），城市 邮编
}
\date{}

\begin{document}

\twocolumn[
  \begin{@twocolumnfalse}
  \maketitle
  \begin{quote}
  \small
  \noindent\textbf{摘\ 要：}大语言模型驱动的 UI 测试自愈正用于降低脆弱定位器的维护成本。本文在 ReproBreak 数据集 koenig 项目 75 个真实 Playwright 可达失效用例上，测量代码模型 qwen2.5-coder:7b 在缺乏行为验证时的行为：78.7\% 对抗场景中模型给出指向错误元素的选择器（即隐性误修复），附加“无匹配则放弃”软提示后结果不变。给出 HealReact 系统 L1 静态基底（AST 抽取、意图标注、确定性校准），静态覆盖 80.6\% 真实失效目标，L3 修复器在可达用例上 top-1 正确率达 77.3\%。结果表明：无人值守持续集成自愈场景下，引入行为重放验证器是必要的下一步。

  \vspace{2pt}
  \noindent\textbf{关键词：}测试自愈；大语言模型；Playwright；React；隐性误修复

  \vspace{2pt}
  \noindent\textbf{中图分类号：}TP311.5\quad\textbf{文献标识码：}A

  \vspace{4pt}
  \noindent\textbf{Abstract:} LLM-driven self-healing for end-to-end UI tests is increasingly used to absorb the maintenance cost of brittle locators. On real-world Playwright break cases from the ReproBreak dataset, across 75 reachable koenig breaks, qwen2.5-coder:7b silently emits a wrong yet resolver-valid Playwright selector in 78.7\% of adversarial-by-construction cases; an explicit ``abstain'' prompt guardrail produces identical numbers. HealReact's L1 substrate (AST extractor + intent labeller + deterministic calibration) reaches 80.6\% of the 93 real break targets statically; on top of it the L3 healer answers 77.3\% of reachable breaks correctly. We make the empirical case that a behavioural-replay oracle is a necessary next layer for unattended CI healing.

  \vspace{2pt}
  \noindent\textbf{Keywords:} software testing; self-healing test; large language model; Playwright; React; abstract syntax tree

  \vspace{6pt}
  \end{quote}
  \end{@twocolumnfalse}
]

\input{sections/01_intro}
\input{sections/02_background}
\input{sections/03_l1_substrate}
\input{sections/04_findings}
\input{sections/05_discussion}
\input{sections/07_conclusion}

\bibliography{references}

\end{document}
