   68 |     test('ignores slash menu on blank paragraph', async function ({page}) {
   69 |         await focusEditor(page);
>> 70 |         await expect(await page.locator('[data-kg-slash-menu]')).toHaveCount(0);
   71 |         await page.keyboard.type('/');
   72 |         await expect(await page.locator('[data-kg-slash-menu]')).toHaveCount(0);
